import { Tree } from "@/types/card_types";

interface PropsType {
    card: Tree | null;
}

const TreeComponent = ({ card }: PropsType) => {
    return (
        <div>
            
        </div>
    );
};

export default TreeComponent;
